---
# node_exporter role metadata

galaxy_info:
  role_name: node_exporter
  author: ansible-proxmox-apps
  description: Install prometheus node_exporter as a pinned-version systemd service on every guest
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: Ubuntu
      versions:
        - jammy
        - noble

  galaxy_tags:
    - prometheus
    - metrics
    - monitoring
    - observability

dependencies: []
