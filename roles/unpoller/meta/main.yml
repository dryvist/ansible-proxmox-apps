---
# unpoller role metadata

galaxy_info:
  role_name: unpoller
  author: ansible-proxmox-apps
  description: Install unpoller (UniFi controller poller) as a pinned-version systemd service
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: Ubuntu
      versions:
        - jammy
        - noble

  galaxy_tags:
    - unifi
    - prometheus
    - metrics
    - monitoring
    - observability

dependencies: []
