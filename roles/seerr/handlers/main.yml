---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Restart seerr
  community.docker.docker_compose_v2:
    project_src: "{{ seerr_data_dir }}"
    state: present
    recreate: always
