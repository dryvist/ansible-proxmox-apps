---
galaxy_info:
  author: JacobPEvans
  description: Deploy Jellyseerr (request UI) as Docker-in-LXC; register Sonarr/Radarr/Plex via its API
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: Ubuntu
      versions:
        - jammy
        - noble

  galaxy_tags:
    - jellyseerr
    - media
    - request
    - docker

dependencies: []
  # community.docker collection is specified in requirements.yml
