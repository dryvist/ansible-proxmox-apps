{{ ansible_managed | comment }}
# OpenBao policy for the monitoring AppRole — netmon/unifi_metrics/
# prometheus_stack and similar. Read-only, no access to the infra kernel or
# other app/platform services.

path "{{ openbao_kv_mount }}/data/apps/monitoring/*" {
  capabilities = ["read"]
}

path "{{ openbao_kv_mount }}/metadata/apps/monitoring/*" {
  capabilities = ["read", "list"]
}

# Off-site deadman ping URL (service_deadman role): a third-party SaaS
# credential, so it lives in secrets-external, not secret/apps.
path "secrets-external/data/platform/healthchecks-offsite" {
  capabilities = ["read"]
}

path "secrets-external/metadata/platform/healthchecks-offsite" {
  capabilities = ["read", "list"]
}
