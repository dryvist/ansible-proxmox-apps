## Managed by Ansible (prometheus_stack role) — do not edit by hand.
## Probe MODULES only. Prometheus supplies the target per scrape via the
## multi-target pattern: /probe?target=<addr>&module=<module-name>. The list of
## targets lives in prometheus.yml (prometheus_stack_blackbox_jobs), not here.
modules:
{{ prometheus_stack_blackbox_modules | to_nice_yaml(indent=2) | indent(2, true) }}
