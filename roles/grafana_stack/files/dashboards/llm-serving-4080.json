{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "preload": false,
  "refresh": "30s",
  "schemaVersion": 42,
  "tags": ["llm", "llama-server"],
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Prompt throughput (tok/s)",
      "description": "llamacpp:prompt_tokens_seconds, scraped directly from llama-server's /metrics (roles/prometheus_stack, job=llama_server).",
      "datasource": { "type": "prometheus", "uid": "victoriametrics" },
      "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "victoriametrics" },
          "editorMode": "code",
          "legendFormat": "{{job}}",
          "expr": "llamacpp:prompt_tokens_seconds{job=\"llama_server\"}"
        }
      ]
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Generation throughput (tok/s)",
      "description": "llamacpp:predicted_tokens_seconds — decode-side throughput, scraped directly from llama-server's /metrics (job=llama_server).",
      "datasource": { "type": "prometheus", "uid": "victoriametrics" },
      "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "victoriametrics" },
          "editorMode": "code",
          "legendFormat": "{{job}}",
          "expr": "llamacpp:predicted_tokens_seconds{job=\"llama_server\"}"
        }
      ]
    },
    {
      "id": 3,
      "type": "timeseries",
      "title": "Requests processing vs deferred",
      "description": "llamacpp:requests_processing (active) and llamacpp:requests_deferred (queued, waiting for a slot), scraped directly from llama-server's /metrics (job=llama_server).",
      "datasource": { "type": "prometheus", "uid": "victoriametrics" },
      "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "victoriametrics" },
          "editorMode": "code",
          "legendFormat": "processing ({{job}})",
          "expr": "llamacpp:requests_processing{job=\"llama_server\"}"
        },
        {
          "refId": "B",
          "datasource": { "type": "prometheus", "uid": "victoriametrics" },
          "editorMode": "code",
          "legendFormat": "deferred ({{job}})",
          "expr": "llamacpp:requests_deferred{job=\"llama_server\"}"
        }
      ]
    }
  ],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "LLM Serving — RTX 4080",
  "uid": "llm-serving-4080",
  "weekStart": "",
  "id": null,
  "version": 1
}
