# {{ ansible_managed }}
# Mirror of nix-ai renderAutonomous `.codex/rules/default.rules`, rendered from
# agent_guest_residual_deny. These rules apply to commands outside Codex's
# filesystem sandbox.
{% for prefix in agent_guest_residual_deny %}
prefix_rule({{ prefix.split(' ') | to_json }}, "forbidden")
{% endfor %}
