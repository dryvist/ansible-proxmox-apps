---
galaxy_info:
  author: JacobPEvans
  description: >-
    MCP gateway (IBM mcp-context-forge) — single HTTP/SSE endpoint every MCP
    client (Claude Code, Codex, Cursor, OpenCode) reaches, instead of each
    connecting to its own bespoke set of MCP servers. Pip-installed, SQLite
    state.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - mcp
    - ai
    - gateway
dependencies: []
