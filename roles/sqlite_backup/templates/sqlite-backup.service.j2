[Unit]
# {{ ansible_managed }}
Description=Online-consistent SQLite backups to the persistent app-config mount

[Service]
Type=oneshot
ExecStart=/usr/local/bin/sqlite-backup.sh
# Local disk I/O only; never let one slow database wedge the timer forever.
TimeoutStartSec=3600
