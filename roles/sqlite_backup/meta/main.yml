---
galaxy_info:
  role_name: sqlite_backup
  author: homelab
  description: >-
    Scheduled online-consistent SQLite backups (SQLite .backup API) written
    beside the live database on the persistent app-config mount, where the
    storage layer's existing snapshot and replication schedule carries them
    off-node.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - backup
    - sqlite

dependencies: []
