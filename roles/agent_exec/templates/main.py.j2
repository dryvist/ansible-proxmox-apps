"""Placeholder agent-exec entrypoint — REPLACE with your CrewAI / LangChain code.

Deployed ONCE (force: false) and never overwritten by Ansible, so your edits
survive re-converges. Keep `import otel_bootstrap` first to keep agent runs
traced. The stub idles so the systemd service stays active until real agent code
lands; swap the idle loop for e.g. Crew(...).kickoff() or your LangChain run.
"""
import time

import otel_bootstrap  # noqa: F401  initialises OpenLLMetry tracing on import


def main() -> None:
    while True:
        time.sleep(3600)


if __name__ == "__main__":
    main()
