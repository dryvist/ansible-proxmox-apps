---
galaxy_info:
  author: JacobPEvans
  description: >-
    Deploy n8n workflow-automation (postgres + n8n app) via Docker Compose
    in an LXC container.
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie

  galaxy_tags:
    - n8n
    - docker
    - automation
    - workflow

dependencies: []
  # community.docker collection is specified in requirements.yml
