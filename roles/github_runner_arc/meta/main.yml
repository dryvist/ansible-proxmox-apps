---
galaxy_info:
  author: JacobPEvans
  description: >-
    Actions Runner Controller (gha-runner-scale-set) on a single-node k3s
    cluster — autoscaling ephemeral GitHub Actions runners, outbound long-poll
    only, alongside the existing systemd github_runner fleet during the pilot.
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie

  galaxy_tags:
    - github
    - actions
    - kubernetes
    - k3s
    - autoscaling

dependencies: []
