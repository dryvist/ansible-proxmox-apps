---
# A vhost change needs no restart: nginx re-reads its configuration on SIGHUP
# and finishes in-flight requests on the old workers.
- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
