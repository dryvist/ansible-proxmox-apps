---
# Cribl Stream Collector schedule: the monitored node's Redfish Power endpoint.
id: bmc_redfish_power
type: collection
collector: bmc_redfish_power
enabled: {{ cribl_stream_bmc_enabled | lower }}
schedule:
  cronSchedule: "{{ cribl_stream_bmc_schedule }}"
  timezone: UTC
  run:
    mode: full
