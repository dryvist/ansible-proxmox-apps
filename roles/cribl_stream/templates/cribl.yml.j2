---
# Worker Process sizing, RAM-derived — see cribl_stream_worker_count in
# roles/cribl_stream/defaults/main.yml for the formula and doc citations.
workers:
  count: {{ cribl_stream_worker_count }}
  memory: {{ cribl_stream_worker_heap_mb }}
