---
- name: Restart openbao voter-health timer
  ansible.builtin.systemd:
    name: "{{ openbao_voter_health_service_name }}.timer"
    state: restarted
    daemon_reload: true
  when: ansible_virtualization_type | default('') != 'docker'
