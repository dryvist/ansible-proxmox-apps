# {{ ansible_managed }}
[Unit]
Description=Daily reclaim of unused Docker/containerd data on the CI data disk

[Timer]
OnCalendar={{ github_runner_prune_schedule }}
Persistent=true

[Install]
WantedBy=timers.target
