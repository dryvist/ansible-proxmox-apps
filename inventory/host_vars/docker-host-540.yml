---
# Second CI guest, on the Xeon Gold node.
#
# Drained: containerd snapshotter corruption on this host breaks every
# image pull (Lchown ENOENT extracting a layer), so no runners on this host
# until it is rebuilt. 0, not removed: the role must still converge here to
# stop and deregister the pool a prior 6-replica converge already started.
github_runner_replicas: 0
github_runner_review_slots: 0
