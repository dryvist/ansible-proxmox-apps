---
# Molecule verification for technitium_dns (no live DNS server).
#
# Re-runs the role with technitium_dns_apply=false to populate the derived facts,
# then asserts the HA topology resolved correctly across the two-node group:
#   - the primary is the lowest-vmid member (technitium10 / .10);
#   - each host's role matches its identity (primary vs secondary);
#   - the secondary-IP list excludes the primary and includes the peer;
#   - inventory A-records were built for both hosts;
#   - the authoritative zone is the subdomain and forwarders point at the gateway.

- name: Verify
  # See converge.yml: leasedguest40-test is inventory-only and must not be a
  # play target.
  hosts: technitium_dns_group
  gather_facts: false

  vars:
    technitium_dns_apply: false
    technitium_dns_host: "{{ container_ip | default(ansible_host) }}"
    tofu_data:
      ingress: []

  tasks:
    # import_role (not include_role) so the role's defaults (zone, apex_domain,
    # forwarders) are exposed to the assertions below, alongside the set_fact
    # outputs (primary_ip, role, secondary_ips, a_records).
    - name: Re-run technitium_dns role to populate derived facts (no live API)
      ansible.builtin.import_role:
        name: technitium_dns

    - name: Assert the primary is the lowest-vmid node (technitium10 / .10)
      ansible.builtin.assert:
        that:
          - technitium_dns_primary_ip == '192.0.2.10'
        fail_msg: >-
          Expected primary 192.0.2.10, got '{{ technitium_dns_primary_ip }}'.

    - name: Assert this host's role matches its identity
      ansible.builtin.assert:
        that:
          - >-
            technitium_dns_role ==
            ('primary' if container_ip == '192.0.2.10' else 'secondary')
        fail_msg: >-
          Role mismatch on {{ inventory_hostname }} ({{ container_ip }}):
          got '{{ technitium_dns_role }}'.

    - name: Assert the secondary-IP list excludes the primary and includes the peer
      ansible.builtin.assert:
        that:
          - "'192.0.2.10' not in technitium_dns_secondary_ips"
          - "'192.0.2.11' in technitium_dns_secondary_ips"
        fail_msg: >-
          Unexpected secondary IPs: {{ technitium_dns_secondary_ips }}.

    - name: Assert an A-record was built for the guest that declares an address
      ansible.builtin.assert:
        that:
          - "'technitium10' in (technitium_dns_a_records | map(attribute='name') | list)"
          - >-
            (technitium_dns_a_records | selectattr('name', 'equalto', 'technitium10')
             | map(attribute='ip') | first) == '192.0.2.10'
        fail_msg: >-
          A-records not built as expected: {{ technitium_dns_a_records }}.

    # A leased guest (leasedguest40) advertises an FQDN, because its address lives
    # only in the DHCP lease and the gateway answers for it. Publishing a record
    # here would be a second copy of that address with nothing keeping the two in
    # agreement, so the builder must skip it entirely. Both resolvers are static
    # and DO belong in the zone — hence a separate fixture host for this case.
    - name: Assert leased guests are skipped, not published with a name
      ansible.builtin.assert:
        that:
          - "'leasedguest40' not in (technitium_dns_a_records | map(attribute='name') | list)"
          - "'technitium70' in (technitium_dns_a_records | map(attribute='name') | list)"
        fail_msg: >-
          a leased guest reached the zone, or a static resolver was dropped from
          it: {{ technitium_dns_a_records }}.

    - name: Assert the query-log export target derives from the syslog CNAME + tofu port
      ansible.builtin.assert:
        that:
          - technitium_dns_query_log_enabled | bool
          - technitium_dns_query_log_syslog_host == 'syslog.svc.example.net'
          # No dns_query family in the molecule tofu_data stand-in -> the
          # guarded default (the family's standard frontend, 522) applies.
          - technitium_dns_query_log_syslog_port | int == 522
          # Role manages ONLY the syslog block (merged over the app's live
          # config at apply time); `enabled` is the real flag (not `enable`).
          - technitium_dns_query_log_syslog.enabled | bool
          - technitium_dns_query_log_syslog.protocol == 'TCP'
          - technitium_dns_query_log_syslog.address == 'syslog.svc.example.net'
          - technitium_dns_query_log_syslog.port | int == 522
        fail_msg: >-
          Query-log export vars did not derive as expected:
          host='{{ technitium_dns_query_log_syslog_host }}'
          port='{{ technitium_dns_query_log_syslog_port }}'
          syslog={{ technitium_dns_query_log_syslog }}.

    - name: Assert authoritative zone is the subdomain and forwarders -> gateway
      ansible.builtin.assert:
        that:
          - technitium_dns_zone == 'svc.example.net'
          - >-
            technitium_dns_forwarders ==
            ["https://cloudflare-dns.com/dns-query (1.1.1.1)",
            "https://dns.google/dns-query (8.8.8.8)"]
        fail_msg: >-
          Zone/forwarder mismatch: zone='{{ technitium_dns_zone }}'
          forwarders={{ technitium_dns_forwarders }}.

    # The apex-zone ingress-alias task (Create Traefik ingress alias apex A
    # records) reuses this SAME derived list and zone by design — one fronted-
    # service name list feeding both the subdomain and apex writes, never a
    # separately maintained apex list. An apex=true row (the Proxmox UI route)
    # must stay excluded here exactly as it already is from the subdomain list.
    - name: Assert the apex-zone ingress alias list/domain match the subdomain alias derivation
      vars:
        tofu_data:
          ingress:
            - { name: grafana, hostname: grafana }
            - { name: proxmox, apex: true }
      ansible.builtin.assert:
        that:
          - technitium_dns_ingress_aliases == ['grafana']
          - technitium_dns_apex_domain == 'example.net'
        fail_msg: >-
          Apex ingress-alias derivation changed: aliases=
          {{ technitium_dns_ingress_aliases }} apex_domain=
          {{ technitium_dns_apex_domain }}.

    # The apex zone has no zone-transfer relationship between resolvers (unlike
    # the subdomain zone, which a secondary replicates via AXFR), so the apex
    # ingress-alias write must run on EVERY resolver, not just the primary —
    # otherwise one resolver keeps serving the stale pre-migration address.
    # No live server exists in CI to exercise the task's `when:`, so this reads
    # the task source directly and asserts the role gate is gone from exactly
    # that task, while confirming sibling tasks that must STAY primary-only
    # (the subdomain zone's own equivalents) still have it — proving the
    # extraction isolated the right task rather than matching too broadly.
    # The whole task tree, not main.yml alone: main.yml is a thin dispatcher
    # now, and the tasks the assertions below inspect live in the files it
    # includes. Reading only the dispatcher makes every regex return nothing —
    # which surfaces as `length` on None rather than as a failed assertion, so
    # the checks it guards never run at all.
    - name: Read the technitium task tree
      ansible.builtin.set_fact:
        technitium_dns_tasks_source: >-
          {{ technitium_dns_tasks_source | default('') ~ lookup('file', item) ~ '\n' }}
      loop: >-
        {{ (query('fileglob', playbook_dir ~ '/../../roles/technitium_dns/tasks/*.yml')
            + query('fileglob', playbook_dir ~ '/../../roles/technitium_dns/tasks/main/*.yml'))
           | sort }}
      loop_control:
        label: "{{ item | basename }}"

    - name: Assert the apex ingress-alias task dropped the primary-only gate
      vars:
        technitium_dns_apex_alias_block: >-
          {{ technitium_dns_tasks_source
             | regex_search('(?s)- name: Create Traefik ingress alias apex A records.*?(?=\n- name:|\Z)') }}
        technitium_dns_subdomain_alias_block: >-
          {{ technitium_dns_tasks_source
             | regex_search('(?s)- name: Create Traefik ingress alias A records\n.*?(?=\n- name:|\Z)') }}
        technitium_dns_secondary_zone_block: >-
          {{ technitium_dns_tasks_source
             | regex_search('(?s)- name: Create Secondary DNS zone \(secondary\).*?(?=\n- name:|\Z)') }}
      ansible.builtin.assert:
        that:
          - technitium_dns_apex_alias_block | length > 0
          - "'technitium_dns_role' not in technitium_dns_apex_alias_block"
          - technitium_dns_subdomain_alias_block | length > 0
          - "'technitium_dns_role' in technitium_dns_subdomain_alias_block"
          - technitium_dns_secondary_zone_block | length > 0
          - "'technitium_dns_role' in technitium_dns_secondary_zone_block"
        fail_msg: >-
          Expected the apex ingress-alias task to have no role gate while its
          subdomain sibling and the secondary-zone task keep theirs.
          apex_block={{ technitium_dns_apex_alias_block }}

    - name: Assert Proxmox endpoint apex A records are built from env
      ansible.builtin.assert:
        that:
          - technitium_dns_proxmox_endpoint_ips == ['192.0.2.10', '192.0.2.11']
          - technitium_dns_proxmox_endpoint_a_records | length == 2
          - >-
            technitium_dns_proxmox_endpoint_a_records
            | map(attribute='domain') | unique | list == ['svc.example.net']
          - >-
            technitium_dns_proxmox_endpoint_a_records
            | map(attribute='ip') | list == ['192.0.2.10', '192.0.2.11']
        fail_msg: >-
          Proxmox endpoint apex records were not built from PROXMOX_IP_ADDRESSES:
          {{ technitium_dns_proxmox_endpoint_a_records }}.

    # Extra CNAME records (verbatim FQDN target): an entry with an empty target
    # (e.g. an unset LLM_LARGE_RUNNER_HOST) must be dropped so converge never
    # POSTs a blank alias.
    - name: Assert extra-CNAME filtering drops empty-target entries
      vars:
        technitium_dns_extra_cname_records:
          - { name: llm-large, target: 'host.example.net' }
          - { name: unset-runner, target: '' }
      ansible.builtin.assert:
        that:
          - >-
            (technitium_dns_extra_cname_records | selectattr('target', 'truthy')
             | list | length) == 1
          - >-
            (technitium_dns_extra_cname_records | selectattr('target', 'truthy')
             | map(attribute='name') | list) == ['llm-large']
        fail_msg: >-
          Extra-CNAME truthy filter did not drop the empty-target entry:
          {{ technitium_dns_extra_cname_records }}.

    # Extra A records (verbatim IPv4 target): blank IPs are skipped the same way
    # so a reserved-address hook can be wired in without risking a broken record.
    - name: Assert extra-A filtering drops empty-ip entries
      vars:
        technitium_dns_extra_a_records:
          - { name: runner-host, ip: '192.0.2.10' }
          - { name: runner-host, zone: 'example.net', ip: '192.0.2.10' }
          - { name: unset-runner, ip: '' }
      ansible.builtin.assert:
        that:
          - >-
            (technitium_dns_extra_a_records | selectattr('ip', 'truthy')
             | list | length) == 2
          - >-
            (technitium_dns_extra_a_records | selectattr('ip', 'truthy')
             | map(attribute='name') | list) == ['runner-host', 'runner-host']
          - >-
            (technitium_dns_extra_a_records | selectattr('ip', 'truthy')
             | selectattr('zone', 'defined') | map(attribute='zone') | list)
            == ['example.net']
        fail_msg: >-
          Extra-A truthy filter did not drop the empty-ip entry:
          {{ technitium_dns_extra_a_records }}.

    # BMC records: the `<node>=<address>` env single source must parse into
    # idrac-<node> entries, and an unset/empty source must yield an empty map
    # rather than a malformed entry — a node with no BMC contributes nothing.
    - name: Assert BMC address parsing yields idrac-<node> entries
      vars:
        parse_bmc: >-
          {{ dict(bmc_source | default('', true)
             | split(',') | map('trim') | reject('equalto', '')
             | map('regex_replace', '\s*=\s*', '=') | map('split', '=') | list) }}
        bmc_records: >-
          {{ dict(parse_bmc.keys() | map('regex_replace', '^', 'idrac-')
                  | zip(parse_bmc.values()))
             | dict2items(key_name='name', value_name='ip') }}
      ansible.builtin.assert:
        that:
          - >-
            (bmc_records | map(attribute='name') | list)
            == (bmc_source | trim | length > 0) | ternary(['idrac-node-a', 'idrac-node-b'], [])
          - >-
            (bmc_records | map(attribute='ip') | list)
            == (bmc_source | trim | length > 0) | ternary(['192.0.2.11', '192.0.2.12'], [])
        fail_msg: >-
          BMC parsing produced {{ bmc_records }} for source '{{ bmc_source }}'.
      # Third case is the operator-spaced spelling; fourth is whitespace-only,
      # which trims to empty and must contribute no records at all.
      loop:
        - "node-a=192.0.2.11, node-b=192.0.2.12"
        - ""
        - "node-a = 192.0.2.11 , node-b = 192.0.2.12"
        - "   "
      loop_control:
        loop_var: bmc_source
