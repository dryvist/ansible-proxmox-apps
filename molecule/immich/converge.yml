---
- name: Converge
  hosts: all
  become: true
  gather_facts: true

  vars:
    # Render all config but skip the live `docker compose up` + health check.
    immich_manage_services: false

  roles:
    - role: immich
