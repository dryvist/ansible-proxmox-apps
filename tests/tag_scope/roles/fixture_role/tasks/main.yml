---
# Fixture only — see tests/tag_scope/fixture_playbook.yml and
# tests/test_openbao_include_role_tag_scoping.py, which is what actually
# runs this. Mirrors roles/openbao/tasks/main.yml's tag shape: one `always`
# prerequisite, two finer phases.
- name: Mark that the always-tagged prerequisite ran
  ansible.builtin.debug:
    msg: "MARKER:ALWAYS_RAN"
  tags:
    - always

- name: Mark that the kv-seed-tagged phase ran
  ansible.builtin.debug:
    msg: "MARKER:KV_SEED_RAN"
  tags:
    - fx_kv_seed

- name: Mark that the rbac-tagged phase ran
  ansible.builtin.debug:
    msg: "MARKER:RBAC_RAN"
  tags:
    - fx_rbac
